#include <bits/stdc++.h>
using namespcae std;

int main () {
    int N ;
    cin >> N;
    string A[N+1];
    int K;
    cin >> K;
    
    for (int i = 1; i <= N + 1; i++) {
        cin >> A[i] ;
    }
    for (int i = 1; i <= N + 1 ;i++ ) {
        cout<<A[i];
    }

}